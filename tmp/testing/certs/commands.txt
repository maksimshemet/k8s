kubectl describe certificaterequest wildcard-cert -n minio

kubectl describe challenge -n minio

kubectl patch challenge wildcard-cert-1-3477053965-88635521 -n minio -p '{"metadata":{"finalizers":[]}}' --type=merge
kubectl delete challenge wildcard-cert-1-3477053965-88635521 -n minio

kubectl create secret generic rndc-key \
  --from-literal=secret== \
  -n cert-manager


root@ns1:/etc/bind# tsig-keygen -a hmac-sha256 cert-manager
key "cert-manager" {
        algorithm hmac-sha256;
        secret "";
};